export enum genderEnum {
  male = 'male',
  female = 'female',
}

export enum statusEnum {
  active = 'active',
  blocked = 'blocked',
}

export enum userInfoSensitiveFieldsEnum {
  userId = 'userId',
  version = '__v',
}
